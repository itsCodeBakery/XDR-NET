# Ablation Study

This folder contains experiment artifacts for XDR-Net ablations.

## Structure
- `APTOS2019_512/` — results for APTOS2019 @ 512×512
- Each run (R0…R4) will create a subfolder with epoch CSVs, a run card, and optional figures.
